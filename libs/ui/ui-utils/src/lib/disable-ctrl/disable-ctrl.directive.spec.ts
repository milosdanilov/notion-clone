import { DisableCtrlDirective } from './disable-ctrl.directive';

describe('DisableCtrlDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableCtrlDirective();
    expect(directive).toBeTruthy();
  });
});
